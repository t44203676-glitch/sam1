# تفعيل إعادة الكتابة
RewriteEngine On

# إعادة توجيه الصفحة الرئيسية
RewriteRule ^$ index.php [L]

# السماح بالوصول للملفات والمجلدات الموجودة
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# إعادة توجيه المسارات النظيفة (اختياري)
# مثال: /about يصبح /pages/about/index.php
# يمكن تفعيل هذا لاحقاً إذا أردت مسارات أنظف

# حماية الملفات الحساسة
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>
